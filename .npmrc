# typia is pinned to v11 (unplugin-typia compatibility), which conflicts with
# some peers under npm's strict resolver — every install here already needs
# --legacy-peer-deps; this makes it the default (incl. `npm ci` in docker).
legacy-peer-deps=true
